// Opponents built on the engine package, and the generic pure game state played against them.
// React glue (`useEngineGame`) lives under the "./react" subpath so this entry stays framework-free.
export * from './game';
export * from './opponent';
